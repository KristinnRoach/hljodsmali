import { createContext } from 'react';

interface MediaSourceCtxType {
  audioBufferRef: React.MutableRefObject<AudioBuffer | null>;
  setNewAudioSrc: (newAudioBuffer: AudioBuffer | Blob) => void;
  setAudioBuffer: React.Dispatch<React.SetStateAction<AudioBuffer | null>>;
}

export const MediaSourceCtx = createContext<MediaSourceCtxType>({
  audioBufferRef: { current: null },
  setNewAudioSrc: () => {},
  setAudioBuffer: () => {},
});

// startRecording: (duration?: number) => void;
// stopRecording: () => void;

// startRecording: () => {
//   // possible to provide default behavior here if needed
// },
// stopRecording: () => {},
