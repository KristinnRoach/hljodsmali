import { createContext } from 'react';

export interface AudioContextType {
  audioCtx: AudioContext | null;
}

const ReactAudioCtx = createContext<AudioContextType>({ audioCtx: null });

export default ReactAudioCtx;
